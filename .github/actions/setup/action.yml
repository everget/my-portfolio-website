name: Setup pnpm + Node
description: Install and cache pnpm + Node dependencies

runs:
    using: composite
    steps:
        - uses: pnpm/action-setup@v5

        - uses: actions/setup-node@v5
          with:
              node-version: 'lts/*'
              cache: 'pnpm'
